# AGENTS.md

This repo will contain multiple numbered project folders (project-1, project-2, project-3, ... up to project-12) as I work through a series of exercises. For any given task, I will always tell you which project number is active. Only ever read, create, or edit files inside that active project's folder. Never open, read, reference, or modify files inside any other project-N folder, even to check for context or reuse code, even if it seems helpful. If a task seems to require touching another project folder, stop and ask instead of proceeding.
